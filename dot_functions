# ~/.functions
serve() {
  python3 -m http.server "${1:-8000}"
}

dr() {
  sudo systemctl restart docker
}

search-text() {
  grep -rnw . -e "$1"
}

fuzzy-search() {
  fzf --preview 'bat --style=numbers --color=always {}'
}
